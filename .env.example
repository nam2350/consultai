# AI 상담 분석 시스템 환경 변수 설정 예시
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 수정하세요

# ============================================================
# 서버 설정
# ============================================================
HOST=0.0.0.0
PORT=8000
DEBUG=true

# ============================================================
# 바운드 키 설정 (센터링크 연동용)
# ============================================================
# 쉼표로 구분하여 여러 키를 등록할 수 있습니다
# 각 키는 최소 20자 이상이어야 합니다
#
# 예시:
# BOUND_KEYS=centerlink_prod_key_2025_abc123,centerlink_dev_key_2025_xyz789
#
# 주의: 프로덕션 환경에서는 반드시 안전한 랜덤 키를 생성하여 사용하세요
# 키 생성 방법: Python에서 secrets.token_urlsafe(24) 사용

BOUND_KEYS=test_key_centerlink_2025,dev_key_12345678901234

# ============================================================
# AI 모델 설정
# ============================================================
# SLM 모델 경로 (실시간 처리용)
SLM_MODEL_PATH=models/Qwen3-1.7B

# LLM 모델 경로 (배치 처리용)
LLM_MODEL_PATH=models/Qwen3-4B

# ============================================================
# CORS 설정
# ============================================================
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:8000

# ============================================================
# 로깅 설정
# ============================================================
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# ============================================================
# 배치 처리 설정
# ============================================================
# 배치 처리 최대 크기
MAX_BATCH_SIZE=20

# 배치 처리 타임아웃 (초)
BATCH_TIMEOUT=600

# ============================================================
# 콜백 설정 (센터링크 연동용)
# ============================================================
# 배치 처리 완료 후 결과를 전송할 센터링크 콜백 URL
# CALLBACK_URL=https://centerlink.example.com/api/callback

# 콜백 재시도 횟수
CALLBACK_RETRY_COUNT=3

# 콜백 재시도 간격 (초)
CALLBACK_RETRY_INTERVAL=5
